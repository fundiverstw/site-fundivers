node_modules
dist
.DS_Store
*.local
.env
.env.*.local
.wrangler
.svelte-kit

# Playwright output
playwright-report
test-results
blob-report

# All env files (dev + mode-specific like .env.production) hold secrets — never commit
.env.*
!.env.example
